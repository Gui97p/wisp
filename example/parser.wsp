struct Point {
    int x, y
    bool visible
    Point *parent
}

func makePoint(int a, b, Point *parent) *Point {
    uint a = 5 + 3 * 2;

    :myFor for i = 1..10:2 {
        continue;
        break myFor;
    }

    return &a, 3 * 3 + a * a;
}